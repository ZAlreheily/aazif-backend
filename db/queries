const sqlite3 = require('sqlite3').verbose();
const db = new sqlite3.Database('./db/KFUPM Events.db');

const getDbConnection = async () => {
    return await sqlite.open({
        filename: './databases/gamee.db3',
        driver: sqlite3.Database
    })
};

